{{#if view.isShowVisible}}
  <td>{{view.content.number}}</td>
  <td>{{view.content.name}}</td>
  <td>{{view.content.position.name}}</td>
  <td>{{view.content.ma}}</td>
  <td>{{view.content.st}}</td>
  <td>{{view.content.ag}}</td>
  <td>{{view.content.av}}</td>
  <td class="skillBox">
    {{#each skill in view.content.position.skills}}
      {{view CalculateYourTV.SkillLabelView contentBinding=skill playerBinding=view.content}}
    {{/each}}
    {{#each skill in view.content.skills}}
      {{view CalculateYourTV.SkillLabelView contentBinding=skill playerBinding=view.content}}
    {{/each}}
  </td>
  <td>{{view.content.cost}}</td>
  <td>
    <a {{action "chooseSkillDialogue" target="view"}} data-toggle="modal" data-target="#modal">Manage Skills</a>
    <a {{action "toggleVisibility" target="view"}}>Edit</a>
    <a {{action "removePlayer" target="view"}}>Delete</a>
  </td>
{{else}}
  <td>{{view.content.number}}</td>
  <td>{{input type="text" value=view.content.name}}</td>
  <td>
    <select>
      {{#each position in view.content.team.roster.positions}}
        {{view CalculateYourTV.PlayerChangeDropdownView contentBinding=position playerBinding=view.content}}
      {{/each}}
    </select>
  </td>
  <td>{{view.content.position.ma}}</td>
  <td>{{view.content.position.st}}</td>
  <td>{{view.content.position.ag}}</td>
  <td>{{view.content.position.av}}</td>
  <td class="skillBox">
    {{#each skill in view.content.position.skills}}
      {{view CalculateYourTV.SkillLabelView contentBinding=skill playerBinding=view.content}}
    {{/each}}
    {{#each skill in view.content.skills}}
      {{view CalculateYourTV.SkillLabelView contentBinding=skill playerBinding=view.content}}
    {{/each}}
  </td>
  <td>{{view.contentBinding.cost}}</td>
  <td><a {{action "toggleVisibility" target="view"}}>Done</a></td>
{{/if}}
